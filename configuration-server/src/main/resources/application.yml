spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/Suvendu-hatua/microservice-poc-config-server-repo.git"
          timeout: 5
          force-pull: true

server:
  port: 9090