spring:
  application:
    name: cart-service
  data:
    redis:
      port: 6379
      connect-timeout: 5s
      host: localhost

server:
  port: 9099

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
        defaultZone: http://localhost:8761/eureka/